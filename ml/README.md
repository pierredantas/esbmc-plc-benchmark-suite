# SLM fine-tuning: props.yaml generation

Fine-tunes a small open model (Qwen2.5-Coder, MLX/LoRA) to generate a
benchmark's `props.yaml` safety properties from its program source. Scope is
deliberately narrow: given one program, emit its properties in the suite's
schema. No verification, no property checking, nothing else.

The `augmented-best` checkpoint (239 examples, iteration 640, the default in
`ml/scripts/generate_props.py`) is published at
[huggingface.co/Pvdantas/esbmc-plc-props-slm-lora](https://huggingface.co/Pvdantas/esbmc-plc-props-slm-lora)
(private) — pull it instead of retraining if you already have access.

## Setup

```bash
python3 -m venv ml/.venv
source ml/.venv/bin/activate
pip install mlx-lm pyyaml jsonschema
```

Apple Silicon only (MLX). `ml/.venv`, `ml/adapters/`, and `ml/data/` are
gitignored — all three are regenerated by the commands below.

## 1. Build the dataset

```bash
python3 ml/scripts/build_dataset.py --augment-st
```

Walks `benchmarks/<domain>/<id>/`, pairs every variant's source with its
`props.yaml`, and writes `ml/data/{train,valid,test}.jsonl` (domain-stratified
split by task id, so a task's variants never straddle splits). Every target is
validated against `schema/properties.schema.json` before inclusion.

`--augment-st` adds a second training pair for every LD-graphical/FBD/SFC
(PLCopen XML) variant: `runner/ld_to_st.py` reconstructs an equivalent ST
rendering from the same LD body, so the same validated ground truth trains the
model on a second concrete syntax for free. Raises the dataset from 152 to 239
records (87 XML variants have a translatable LD body; 13 are pure FBD/SFC with
none). Omit the flag to build the smaller, unaugmented set.

## 2. Fine-tune

```bash
python3 -m mlx_lm lora -c ml/lora_config.yaml             # 152 examples
python3 -m mlx_lm lora -c ml/lora_config_augmented.yaml   # 239 examples (ST-augmented)
```

LoRA rank 16 on Qwen2.5-Coder-1.5B-Instruct-4bit, ~15-45 min on an M-series
Mac. Watch validation loss across checkpoints (`save_every` in each config) —
it plateaus and then rises well before `iters` is exhausted on a dataset this
size, so **pick the checkpoint with the lowest val loss, not the last one**.
Copy the winning `NNNNNNN_adapters.safetensors` plus `adapter_config.json`
into a fresh directory (`mlx_lm.load(adapter_path=...)` always reads
`adapters.safetensors` from whatever directory you point it at):

```bash
mkdir ml/adapters/<name>
cp ml/adapters/<run>/0000NNN_adapters.safetensors ml/adapters/<name>/adapters.safetensors
cp ml/adapters/<run>/adapter_config.json ml/adapters/<name>/
```

## 3. Generate props.yaml for a program

```bash
python3 ml/scripts/generate_props.py path/to/program.st --domain motor_control --language ST
python3 ml/scripts/generate_props.py path/to/program.st --domain motor_control --language ST -o path/to/props.yaml
```

Defaults to the `qwen2.5-coder-1.5b-props-augmented-best` checkpoint; override
with `--adapter-path` to try another. `--language` accepts `ST`,
`LD-textual`, `LD-graphical`, `FBD`, `SFC`, `IL` (a prompt hint, not a parser).

To use the published checkpoint instead of training your own, download it and
point `--adapter-path` at the local copy:

```bash
python3 -c "from huggingface_hub import snapshot_download; print(snapshot_download('Pvdantas/esbmc-plc-props-slm-lora'))"
python3 ml/scripts/generate_props.py path/to/program.st --domain motor_control --language ST \
  --adapter-path <path printed above>
```

## 4. Evaluate

```bash
python3 ml/scripts/evaluate.py --out ml/data/eval_base.json                                          # base model, no adapter
python3 ml/scripts/evaluate.py --adapter-path ml/adapters/<name> --out ml/data/eval_<name>.json       # fine-tuned
```

Runs every `ml/data/test.jsonl` example through the model (greedy decoding)
and scores: `yaml_valid`, `schema_valid` (against `properties.schema.json`),
`id_format_ok`, and `kind_recall`/`kind_precision` (multiset overlap of
`kind` values against the expected properties).

## Results so far (1.5B, both checkpoints use the lowest-val-loss iteration)

| Metric | Base (no fine-tune) | Fine-tuned, 152 examples | Fine-tuned, 239 examples (ST-augmented) |
|---|---|---|---|
| Valid YAML | 59% | 100% | 97% |
| Schema-valid | 0% | 91% | 97% |
| Correct id format | 59% | 100% | 97% |
| Property-kind recall | 18% | 73% | 73% |
| Property-kind precision | 3% | 73% | 73% |

Test splits differ in size (22 vs. 33 examples) because augmentation changes
task-to-split assignment, so treat this as directional, not a controlled A/B.

**Known failure modes, both checkpoints:**
- **Under-enumeration.** A program with two independent safety intents often
  gets only one property back. Neither checkpoint reliably enumerates every
  interlock a program supports.
- **Kind confusion on two-variable properties**, in opposite directions per
  checkpoint. Every `mutual_exclusion` example in the training data is a
  symmetric output-output pair (two motors, two valves, two contactors); every
  `invariant` example with 2+ variables is dominated by asymmetric
  cause-effect pairs (one sensor/alarm, one actuator). The 152-example model
  sometimes picks `mutual_exclusion` for a cause-effect pair (wrong). The
  239-example (ST-augmented) model swung the other way and sometimes picks
  `invariant` for a symmetric peer-actuator pair where `mutual_exclusion` is
  the dataset's own convention. Neither model has reliably learned the
  underlying variable-role distinction; augmentation shifted the bias, it
  did not remove it.
- **Variable hallucination**, seen once (152-example checkpoint, not yet
  reproduced on the augmented one): a generated `expression` referenced a
  variable that does not appear anywhere in the source program.
- **Zero training examples** for `kind: absence` and `kind: assertion` — both
  are valid per the schema but unused anywhere in the current 50-benchmark
  corpus, so neither model has ever seen one and will not produce one.

## Next levers, roughly in order of expected payoff

1. **More real, validated benchmarks** — the dataset is the bottleneck, not
   model size; validation loss plateaus well before training loss does on
   both checkpoints. Prioritize multi-property programs (fixes
   under-enumeration) and asymmetric cause-effect two-variable programs
   outside `chemical_batch` (gives the kind-confusion pattern more contexts
   to generalize from). Adding `absence`/`assertion` examples requires
   authoring new benchmarks; augmentation cannot manufacture a kind that
   does not exist in the source data.
2. **A deterministic post-generation check** — cheap and independent of
   which checkpoint is default: given the source program's declared I/O
   roles (`AT %IX`/`AT %QX`, `VAR_INPUT`/`VAR_OUTPUT`, or PLCopen
   `<inputVars>`/`<outputVars>`), flag a `mutual_exclusion` property whose
   variables are not both outputs, or an `expression`/`variables` list
   referencing a name absent from the program, for human review before the
   generated `props.yaml` is trusted.
3. **A larger base model** (7B) — plausible but secondary; the diagnosed
   bottleneck so far is data coverage, not model capacity.
