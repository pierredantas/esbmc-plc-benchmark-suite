format_version: "0.1"
id: conflict_monitor_trip_gm
name: "Traffic conflict monitor trip and latch"
domain: traffic
language: LD-textual
source:
  origin: authored
  reference: "Authored by Pierre for the esbmc-plc-benchmark-suite"
  license: CC-BY-4.0
properties_file: props.yaml
validation_status: candidate
variants:
  - file: clean.ld
    expected_verdict: true
    ground_truth:
      method: expert
      confirmed_by: [Pierre]
  - file: bomb.ld
    expected_verdict: false
    violated_properties: [P1]
    ground_truth:
      method: fault-injection
      base_variant: clean.ld
      seeded_defect: "Modified the latch logic so sys_reset overrides conflict_detect, allowing the intersection to return to normal operation even if the fault is still physically present."
      witness: "conflict_detect=1, sys_reset=1, req_green=1 reaches solid_green=1 AND conflict_detect=1."
features: [contacts, coils, latch, interlock]
difficulty: easy
tags: [safety, traffic, conflict_monitor, known-issue-esbmc-7577]