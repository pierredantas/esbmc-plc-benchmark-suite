format_version: "0.1"
properties:
  - id: P1
    kind: invariant
    expression: "!(veh_green && train_detect)"
    justification: "Vehicle green light must never energize while a train is physically detected in the crossing island."
  - id: P2
    kind: reachability
    expression: "veh_green && train_detect"
    justification: "Trigger synthesis: a witness recovers the premature reset condition that allows a conflicting green light."
